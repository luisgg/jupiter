diff -uNr jupiter-0.1.4-1~webupd8/debian/postinst jupiter-0.1.4-1~webupd8.patched/debian/postinst
--- jupiter-0.1.4-1~webupd8/debian/postinst	2012-01-17 16:07:53.000000000 +0100
+++ jupiter-0.1.4-1~webupd8.patched/debian/postinst	2012-08-28 12:44:20.090705910 +0200
@@ -20,18 +20,19 @@
 
 case "$1" in
     configure)
-      if [ ! "$(grep jupiter /etc/group)" ]; then
+      if [ ! "$(getent group jupiter >/dev/null)" ]; then
         echo "Adding Jupiter group"
         /usr/sbin/groupadd jupiter
       fi
-      echo -n "Adding users to Jupiter group: "
-      for i in $(awk -F: '$6 ~ /\/home/ && $3 >= 1000 {print $1}' /etc/passwd);
-      do
-        echo -n $i" "
-        usermod -G jupiter -a $i 2>&1 >/dev/null ||:
-      done
+      # echo -n "Adding users to Jupiter group: "
+      # for i in $(awk -F: '$6 ~ /\/home/ && $3 >= 1000 {print $1}' /etc/passwd);
+      # do
+      #   echo -n $i" "
+      #   usermod -G jupiter -a $i 2>&1 >/dev/null ||:
+      # done
       echo -e "\nAdding jupiter to sudoers"
-      grep jupiter /etc/sudoers >/dev/null 2>&1 || echo "%jupiter ALL=NOPASSWD: /usr/lib/jupiter/scripts/bluetooth, /usr/lib/jupiter/scripts/camera, /usr/lib/jupiter/scripts/cpu-control, /usr/lib/jupiter/scripts/resolutions, /usr/lib/jupiter/scripts/rotate, /usr/lib/jupiter/scripts/touchpad, /usr/lib/jupiter/scripts/vga-out, /usr/lib/jupiter/scripts/wifi" >> /etc/sudoers
+      echo "ALL ALL=NOPASSWD: /usr/lib/jupiter/scripts/bluetooth, /usr/lib/jupiter/scripts/camera, /usr/lib/jupiter/scripts/cpu-control, /usr/lib/jupiter/scripts/resolutions, /usr/lib/jupiter/scripts/rotate, /usr/lib/jupiter/scripts/touchpad, /usr/lib/jupiter/scripts/vga-out, /usr/lib/jupiter/scripts/wifi" >> /etc/sudoers.d/10_jupiter
+      chmod 0440 /etc/sudoers.d/10_jupiter
       echo "Setting permissions.."
       chown -R root:jupiter /usr/lib/jupiter
       chown -R root:root /etc/acpi/events
@@ -42,7 +43,7 @@
 
       if [ -d "/var/jupiter" ]; then
 	chown -R root:jupiter /var/jupiter
-        chmod -R 775 /var/jupiter
+        chmod -R 777 /var/jupiter
       fi
 
       if [ -e "/usr/lib/jupiter/scripts/jupiter" ]; then
diff -uNr jupiter-0.1.4-1~webupd8/debian/prerm jupiter-0.1.4-1~webupd8.patched/debian/prerm
--- jupiter-0.1.4-1~webupd8/debian/prerm	1970-01-01 01:00:00.000000000 +0100
+++ jupiter-0.1.4-1~webupd8.patched/debian/prerm	2012-08-28 12:44:30.914759704 +0200
@@ -0,0 +1,44 @@
+#!/bin/sh
+# prerm script
+#
+# see: dh_installdeb(1)
+
+set -e
+
+# summary of how this script can be called:
+#        * <prerm> `remove'
+#        * <old-prerm> `upgrade' <new-version>
+#        * <new-prerm> `failed-upgrade' <old-version>
+#        * <conflictor's-prerm> `remove' `in-favour' <package> <new-version>
+#        * <deconfigured's-prerm> `deconfigure' `in-favour'
+#          <package-being-installed> <version> `removing'
+#          <conflicting-package> <version>
+# for details, see http://www.debian.org/doc/debian-policy/ or
+# the debian-policy package
+
+
+case "$1" in
+    remove|upgrade|deconfigure)
+	rm -f /etc/sudoers.d/10_jupiter
+	if [ -x "/usr/sbin/update-rc.d" ] && [ -e "/etc/init.d/ondemand" ]; then
+		echo "Restoring startup links to ondemand CPU mode"
+		update-rc.d ondemand               start 99 2 3 4 5 . >/dev/null || true
+	fi
+    ;;
+
+    failed-upgrade)
+    ;;
+
+    *)
+        echo "prerm called with unknown argument \`$1'" >&2
+        exit 1
+    ;;
+esac
+
+# dh_installdeb will replace this with shell code automatically
+# generated by other debhelper scripts.
+
+#DEBHELPER#
+
+exit 0
+
